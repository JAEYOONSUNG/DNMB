% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparative_cgc.R
\name{dnmb_plot_comparative_cgc}
\alias{dnmb_plot_comparative_cgc}
\title{Comparative CGC (CAZyme Gene Cluster) Signature Heatmap Across Genomes}
\usage{
dnmb_plot_comparative_cgc(
  data_root,
  output_dir = NULL,
  output_file = "Comparative_CGC_Heatmap.pdf",
  color_palette = c("white", "#006064"),
  bar_color = "#0097A7",
  line_width = 1,
  line_col = "grey80",
  auto_run_missing = TRUE,
  module_cache_root = NULL,
  module_install = TRUE,
  module_cpu = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data_root}{Directory containing one subfolder per genome (each
subfolder may hold DefenseFinder results directly, or one level deeper
under a wrapper such as \code{fullrun_*/}).}

\item{output_dir}{Where to save the PDF. Defaults to
\code{file.path(data_root, "comparative")}.}

\item{output_file}{PDF file name.}

\item{color_palette}{Two-color gradient for heatmap dots.}

\item{bar_color}{Diversity barplot fill color.}

\item{line_width, line_col}{Grid line styling.}

\item{auto_run_missing}{If TRUE, folders with a GenBank record but no
DefenseFinder results are run through \code{run_module_set(db = "DefenseFinder")}.}

\item{module_cache_root, module_install, module_cpu}{Passed through to
\code{run_module_set()} when auto-running.}

\item{verbose}{Print progress.}
}
\value{
Invisibly, a list with \code{pdf}, \code{xlsx}, \code{matrix},
and \code{genomes}.
}
\description{
Walks a directory of per-genome analysis folders, reads each genome's
\code{dnmb_module_dbcan/run_dbcan/cgc_standard_out_summary.tsv}, builds
a compound signature label for every CGC from which signature classes
are present (\code{CAZyme}, \code{TC}, \code{TF}, \code{STP},
\code{Sulfatase}, \code{Peptidase}), and renders a ComplexHeatmap of
signature-mix counts. CAZyme is always present by definition, so the
label is always led by \code{CAZyme}.
}
\details{
Uses the same layout as \code{dnmb_plot_comparative_defensefinder()}.
Shares the dbCAN module auto-run with
\code{dnmb_plot_comparative_dbcan()}.
}
