% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparative_phispy.R
\name{dnmb_plot_comparative_phispy}
\alias{dnmb_plot_comparative_phispy}
\title{Comparative PhiSpy Prophage Heatmap Across Genomes}
\usage{
dnmb_plot_comparative_phispy(
  data_root,
  output_dir = NULL,
  output_file = "Comparative_PhiSpy_Heatmap.pdf",
  color_palette = c("white", "#330066"),
  bar_color = "#4C1C7E",
  line_width = 1,
  line_col = "grey80",
  auto_run_missing = TRUE,
  module_cache_root = NULL,
  module_install = TRUE,
  module_cpu = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data_root}{Directory containing one subfolder per genome (each
subfolder may hold DefenseFinder results directly, or one level deeper
under a wrapper such as \code{fullrun_*/}).}

\item{output_dir}{Where to save the PDF. Defaults to
\code{file.path(data_root, "comparative")}.}

\item{output_file}{PDF file name.}

\item{color_palette}{Two-color gradient for heatmap dots.}

\item{bar_color}{Diversity barplot fill color.}

\item{line_width, line_col}{Grid line styling.}

\item{auto_run_missing}{If TRUE, folders with a GenBank record but no
DefenseFinder results are run through \code{run_module_set(db = "DefenseFinder")}.}

\item{module_cache_root, module_install, module_cpu}{Passed through to
\code{run_module_set()} when auto-running.}

\item{verbose}{Print progress.}
}
\value{
Invisibly, a list with \code{pdf}, \code{xlsx}, \code{matrix},
and \code{genomes}.
}
\description{
Walks a directory of per-genome analysis folders, collects each genome's
\code{dnmb_module_phispy/prophage_coordinates.tsv}, and renders a single
ComplexHeatmap of prophage counts bucketed by region size. Uses the same
layout as \code{dnmb_plot_comparative_defensefinder()}.
}
\details{
PhiSpy does not categorise prophages natively, so size (in bp) is used as
the subtype axis: Small (<10 kb), Medium (10-30 kb), Large (30-60 kb),
Very Large (>=60 kb).
}
